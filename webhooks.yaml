enabled: true
indiviual_hooks: false
webhook_url(s):

hooks:
  - onPageCreated:
  - onPageModified:
  - onPluginAdded:
  - onPluginUpdated:
  - onThemeChanged:

collection_filter: false
collections:

request: '{"text": "This is a line of text.\nAnd this is another one."}'
